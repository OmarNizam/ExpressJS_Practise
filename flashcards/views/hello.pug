extends layout.pug

block content
  //- if name is difined show the welcoming message
  if name
    h2 Welcome, #{name}!
  else
    form(action='/hello', method='post')
      label Please enter you name:
        input(type='text', name='username')
      button(type='submit') Submit
